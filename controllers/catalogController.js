/*
The controller for the browsing of documents under Data/Documents. 

This controller should allow the user to get a listing of all files under the
Documents directory. It should allow the user to upload a file to the Documents 
directory. It should also allow the user to retrieve an individual file
to read in their browser or download it directly for later reading. 


*/

var documentController = function() {

    //The uploadFile method allows the user to upload a file from their browser to the 
    //Documents directory in the Azure storage account. 
    var uploadFile = function() {

    }

    var getAllListings = function() {

    }

    var getFile = function(filename) {

    }

}